{
  "name": "slopspot-paste",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "wrangler types",
    "prebuild": "wrangler types",
    "pretypecheck": "wrangler types",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist --kv PASTES",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "astro": "^6.3.3",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.63.0"
  }
}
